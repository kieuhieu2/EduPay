package com.edu.kindergarten.repository;

import com.edu.kindergarten.entity.DeductionCost;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DeductionCostRepository extends JpaRepository<DeductionCost, Integer> {
}
